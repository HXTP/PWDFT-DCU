grep "Time for the eigensolver" statfile.0
grep "Time for computing density in PWDFT is" statfile.0
grep wall statfile.0
grep "Total time for the SCF iteration" statfile.0




rm nd slurm-*.out  statfile.0 STRUCTURE struct_input 

rm -rf  job-* last* MD.xyz runlog statfile.0 STRUCTURE DEN EveryVel.out npy_data raw_data

rm nohup.out log.* runlog.out *.dump *.rdf